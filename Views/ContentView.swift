import SwiftUI

struct ContentView: View {
    var body: some View {
        HomeView() // Present HomeView as the main view
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
